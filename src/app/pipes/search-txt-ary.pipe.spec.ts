import { SearchTxtAryPipe } from './search-txt-ary.pipe';

describe('SearchTxtAryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTxtAryPipe();
    expect(pipe).toBeTruthy();
  });
});
